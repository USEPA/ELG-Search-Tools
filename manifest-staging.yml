---
 applications:
   - name: elg-search-tools-stage-test
     instances: 1
     memory: 512M
     disk_quota: 1GB
     timeout: 180
     buildpacks:
      - nodejs_buildpack
     command: npm run prod
     command: |
       npm run migrate
       npm run seed
       npm run prod
     env:
        NODE_ENV: staging
        LOGGER_LEVEL: ERROR
        OPTIMIZE_MEMORY: true
     services:
      - elg-db-service
